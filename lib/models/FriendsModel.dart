class FriendsModel {
  final String name;
  final String profileImageUrl;
  final String userid;
  final bool isPending;

  const FriendsModel(
      this.name, this.profileImageUrl, this.userid, this.isPending);
}
